import { View, Text } from 'react-native';

const TrackingsScreen = () => {
  return (
    <View style={{ flex: 1, paddingTop: 12, paddingHorizontal: 10 }}>
      <Text style={{ fontSize: 18,color:'blue' }}>Tracking Screen</Text>
    </View>
  );
};

export default TrackingsScreen;